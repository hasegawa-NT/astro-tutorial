// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://bright-blancmange-d42712.netlify.app/', // ここにNetlifyのサイトURLを入力
});
